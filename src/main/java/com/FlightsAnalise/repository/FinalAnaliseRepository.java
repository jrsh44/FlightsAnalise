package com.FlightsAnalise.repository;

import com.FlightsAnalise.model.FinalAnalise;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FinalAnaliseRepository extends JpaRepository<FinalAnalise, Integer> {
}
